version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      DB_PATH: /app/data/app.db
      PORT: "8080"
    volumes:
      - sqlite_data:/app/data
      - uploads_data:/app/uploads

volumes:
  sqlite_data:
  uploads_data:
